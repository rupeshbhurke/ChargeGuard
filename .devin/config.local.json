{
  "permissions": {
    "allow": [
      "Exec(dotnet new)",
      "Exec(dotnet sln)",
      "Exec(mkdir)",
      "Exec(dotnet restore)",
      "Exec(dotnet build)",
      "Exec(dotnet test)",
      "Exec(dotnet publish)",
      "Exec(powershell)",
      "Exec(git init)",
      "Exec(git add)",
      "Exec(git commit)",
      "Exec(git branch)",
      "Exec(git push)",
      "Exec(git status)",
      "Exec(git log)",
      "Exec(Get-EventLog)",
      "Exec(Start-Process)",
      "Exec(Start-Sleep)",
      "Exec(Get-Process)",
      "Exec(Select-Object)",
      "Exec(Stop-Process)",
      "Exec(Get-Content)"
    ]
  }
}